<div class="min-h-screen bg-gradient-to-br from-slate-50 to-slate-100 py-8">
  <div class="container mx-auto px-4">
    <div class="max-w-2xl mx-auto">
      <div class="bg-white rounded-2xl shadow-xl border border-slate-200 overflow-hidden">
        <div class="bg-gradient-to-r from-blue-600 to-purple-600 px-6 py-4">
          <h1 class="text-xl font-semibold text-white">Edit Category</h1>
        </div>
        <div class="p-6">
          <%= render 'form', category: @category %>
          <%= link_to 'Back to Categories', admin_categories_path, class: 'inline-flex items-center gap-2 px-6 py-3 mt-6 text-slate-600 hover:text-slate-900 hover:bg-slate-100 rounded-xl transition-all duration-200 font-medium border border-slate-300' %>
        </div>
      </div>
    </div>
  </div>
</div>